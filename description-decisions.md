# Description Review Decisions

> Generated by review-descriptions.mjs — feed this file to Claude Code to apply.

## `src/utils.ts`

- **ACCEPT** `WithId.id`

- **EDIT** `WithActor.actor` — replace TSDoc comment with:
  > The person who carried out this activity.
  > @see crm:P14 carried out by

- **ACCEPT** `WithNote.note`

## `src/Symbol.ts`

- **ACCEPT** `Symbol` (interface)

- **EDIT** `Symbol.carriers` — replace TSDoc comment with:
  > References to the physical features (e.g. holes) on the roll copies
  > that carry this symbol. Since the association might be debatable,
  > it can be annotated. There might be e.g. doubts about the meaning
  > and correct transcription of a feature on a physical roll.
  > This points to a feature by its `@id`.
  > @see crm:P128i is carried by

- **EDIT** `Perforation` (interface) — replace TSDoc comment with:
  > A perforation is a symbol that is typically encoded as a single punched
  > hole or a group of punched holes in the physical carrier.
  > However, it might also have different physical appearences.
  > @see reo:C5 Perforation

- **EDIT** `Perforation.alignedWith` — replace TSDoc comment with:
  > In piano rolls, perforations are often aligned with other
  > perforations, e.g. a "crescendo off" might be logically
  > aligned to the start of a note perforation.
  > @see reo:P3 aligned with

- **EDIT** `Note` (interface) — replace TSDoc comment with:
  > A note symbol, representing a single pitched musical event on the roll.
  > The pitch is encoded via the tracker bar position (track number).
  > @see reo:P6 Note

- **EDIT** `Note.pitch` — replace TSDoc comment with:
  > The MIDI pitch number of the note (e.g. 60 for middle C).
  > @see reo:P5 has pitch

- **EDIT** `ExpressionScope` (type) — replace TSDoc comment with:
  > The scope of an expression perforation, indicating whether it
  > applies to the bass or treble register of the piano.
  > @see reo:P6 has scope

- **EDIT** `ExpressionType` (type) — replace TSDoc comment with:
  > The type of expression encoded by a perforation on the roll,
  > e.g. "crescendo on", "crescendo off", etc. on Welte T-100 
  > rolls.
  > @see crm:P2 has type

- **EDIT** `Expression` (interface) — replace TSDoc comment with:
  > An expression symbol, representing a perforation on the roll
  > that governs dynamics, pedaling, or mechanical functions of the
  > reproducing piano. Each expression has a scope (bass or treble)
  > and a specific expression type.
  > @see reo:C7 Expression

- **EDIT** `Expression.scope` — replace TSDoc comment with:
  > Whether this expression applies to the bass or treble register.
  > @see reo:P6 has scope

- **EDIT** `Expression.expressionType` — replace TSDoc comment with:
  > The specific type of expression (e.g. sustain pedal, forzando, etc.).
  > @see crm:P2 has type

- **ACCEPT** `Text` (interface)

- **ACCEPT** `Text.text`

- **ACCEPT** `AnySymbol` (type)

## `src/Feature.ts`

- **ACCEPT** `HorizontalSpan` (interface)

- **EDIT** `HorizontalSpan.unit` — replace TSDoc comment with:
  > The unit of measurement for horizontal positions.
  > Always 'mm' (millimeters).
  > @see crm:P91 has unit

- **ACCEPT** `HorizontalSpan.from`

- **ACCEPT** `HorizontalSpan.to`

- **ACCEPT** `VerticalSpan` (interface)

- **EDIT** `VerticalSpan.unit` — replace TSDoc comment with:
  > The unit of measurement for horizontal positions.
  > @see crm:P91 has unit

- **ACCEPT** `VerticalSpan.to`

- **EDIT** `RollFeature` (interface) — replace TSDoc comment with:
  > A physical feature on the roll, e.g. a perforation, a tear, a mark, etc.,
  > defined by its horizontal and vertical position and extent.
  > @see reo:C2 Feature

- **EDIT** `RollFeature.depiction` — replace TSDoc comment with:
  > IIIF region pointing to a depiction of this feature in the scan.
  > @see crm:P62 is depicted by

- **EDIT** `RollFeature.horizontal` — replace TSDoc comment with:
  > Horizontal span of the feature on the roll.
  > @see crm:P43 has dimension

- **EDIT** `RollFeature.vertical` — replace TSDoc comment with:
  > Vertical span of the feature on the roll.
  > @see crm:P43 has dimension

- **EDIT** `Hole` (interface) — replace TSDoc comment with:
  > A hole (perforation) in the roll paper. Holes are the primary
  > carriers of musical information on piano rolls, as they trigger
  > notes and expression controls when passing over the tracker bar.

- **EDIT** `Hole.pattern` — replace TSDoc comment with:
  > The punching pattern of the hole. Regular holes have evenly-spaced
  > bridges, accelerating holes have decreasing bridge widths, and
  > staggering holes alternate between adjacent tracks (cf. Phillips).

- **ACCEPT** `Trace` (interface)

- **ACCEPT** `WritingMethod` (type)

- **ACCEPT** `Writing` (interface)

- **EDIT** `Writing.method` — replace TSDoc comment with:
  > The method by which this writing was produced
  > (e.g. through print, handwriting, or stamping).

- **ACCEPT** `Writing.transcription`

- **ACCEPT** `Mark` (interface)

- **ACCEPT** `GluedOn` (interface)

- **ACCEPT** `GluedOn.material`

- **ACCEPT** `GluedOn.features`

- **ACCEPT** `AnyFeature` (type)

## `src/Assumption.ts`

- **EDIT** `Certainty` (type) — replace TSDoc comment with:
  > Certainty levels for beliefs, ranging from 'true' to 'false'
  > and some values in between.

- **EDIT** `Argumentation` (interface) — replace TSDoc comment with:
  > An argumentation provides reasons for a belief and
  > may be associated with a person carrying out that argumentation.
  > @see crminf:I1 Argumentation

- **EDIT** `MeaningComprehension` (interface) — replace TSDoc comment with:
  > A meaning comprehension interprets or disambiguates the meaning of
  > symbols or features. For example, interpreting a pencil mark
  > as an instruction to add or remove a perforation or as the dating
  > of the roll.

- **EDIT** `MeaningComprehension.comprehends` — replace TSDoc comment with:
  > References (by `@id`) to the symbols or features
  > whose meaning is being interpreted.
  > @see crminf:J22 interpreted meaning of

- **EDIT** `Inference` (interface) — replace TSDoc comment with:
  > An inference draws a conclusion from given premises.
  > @see crminf:I5 Inference Making

- **EDIT** `Inference.premises` — replace TSDoc comment with:
  > References (by `@id`) to the beliefs or facts
  > from which the conclusion is drawn.
  > @see crminf:J1 used as premise

- **EDIT** `BeliefAdoption` (interface) — replace TSDoc comment with:
  > A belief adoption adopts someone else's belief. This type is used
  > to indicate e.g. knowledge through private communication or
  > from secondary literature.
  > @see crminf:I7 Belief Adoption

- **EDIT** `BeliefAdoption.note` — replace TSDoc comment with:
  > A note describing the source of the adopted belief,
  > e.g. a bibliographic reference or personal communication.
  > @see crm:P3 has note

- **ACCEPT** `AnyArgumentation` (type)

- **EDIT** `Belief` (interface) — replace TSDoc comment with:
  > A belief is a temporal object and associates a proposition (i.e.
  > a statement) with a certainty. It comes into existence through
  > argumentations (reasons). 
  > @see crminf:I2 Belief

- **EDIT** `Belief.certainty` — replace TSDoc comment with:
  > The level of certainty associated with this belief.
  > @see crminf:J5 holds to be

- **EDIT** `Belief.reasons` — replace TSDoc comment with:
  > The argumentations providing reasons for this belief.
  > @see crminf:J2 was concluded by

- **ACCEPT** `Assumption` (interface)

- **EDIT** `Assumption['@annotation']` — replace TSDoc comment with:
  > An optional annotation expressing a belief about this assumption.
  > Uses the JSON-LD-star `@annotation` mechanism to attach
  > epistemic metadata (certainty and reasons) to any triple.

- **EDIT** `ValueAssumption` (interface) — replace TSDoc comment with:
  > A value assumption wraps a literal value, e.g. a string, a number, a date, with an optional annotation.
  > Used for properties where the value itself may be uncertain,
  > e.g. dates or alignment references.

- **ACCEPT** `ValueAssumption['@value']`

- **ACCEPT** `ReferenceAssumption` (type)

- **ACCEPT** `ObjectAssumption` (type)

## `src/Edit.ts`

- **ACCEPT** `EditType` (type)

- **ACCEPT** `ActorAssignment` (type)

- **EDIT** `Edit` (interface) — replace TSDoc comment with:
  > A set of edits transforms a version of a roll into another version.
  > Edits insert or delete symbols, or both (= replace).
  > Edits may be motivated by a given set of reasons, e.g.
  > to add an additional accent or to correct an error.
  > If an edit is the interpretation of a metamark,
  > such as a pencil mark, this should be made explicit
  > using a meaning comprehension on the `@annotation` field.
  > @see reo:C8 Edit

- **ACCEPT** `Edit.editType`

- **EDIT** `Edit.motivation` — replace TSDoc comment with:
  > A textual description of the motivation for this edit,
  > referencing a motivation defined in the version's motivations list.
  > @see crm:P17 was motivated by

- **EDIT** `Edit.insert` — replace TSDoc comment with:
  > The symbols to be inserted by this edit.
  > @see reo:P7 added

- **EDIT** `Edit.delete` — replace TSDoc comment with:
  > References (by `@id`) to the symbols to be deleted by this edit.
  > @see reo:P8 removed

## `src/Version.ts`

- **ACCEPT** `VersionType` (type)

- **EDIT** `Motivation` (type) — replace TSDoc comment with:
  > A motivation provides a reason or rationale for an editorial change.
  > Motivations are defined at the version level and referenced by edits.
  > @see crm:E73 Information Object

- **ACCEPT** `Version.edits`

- **ACCEPT** `Version.motivations`

## `src/RollCopy.ts`

- **ACCEPT** `PaperStretch.factor`

- **EDIT** `GeneralRollCondition` (interface) — replace TSDoc comment with:
  > A general condition description for a roll copy, e.g.
  > overall wear, discoloration, or other observations.
  > @see crm:E3 Condition State

- **ACCEPT** `RollConditionAssignment` (type)

- **ACCEPT** `Shift` (interface)

- **ACCEPT** `Shift.horizontal`

- **ACCEPT** `Shift.vertical`

- **ACCEPT** `DateAssignment` (type)

- **EDIT** `ProductionEvent` (interface) — replace TSDoc comment with:
  > Describes the production of a roll copy, including the
  > manufacturing company, the roll system, and the paper used.
  > @see lrm:F33 Reproduction Event

- **EDIT** `ProductionEvent.company` — replace TSDoc comment with:
  > The company that produced the roll copy
  > (e.g. "M. Welte & Söhne").
  > @see crm:P14 carried out by

- **ACCEPT** `ProductionEvent.system`

- **EDIT** `ProductionEvent.paper` — replace TSDoc comment with:
  > The paper type used for the roll copy.
  > @see P126:employed

- **EDIT** `ProductionEvent.date` — replace TSDoc comment with:
  > The date of production, if known.
  > @see crm:P4 has time-span

- **ACCEPT** `RollCopy` (interface)

- **ACCEPT** `RollCopy.ops`

- **ACCEPT** `RollCopy.measurements`

- **ACCEPT** `measurements.dimensions`

- **ACCEPT** `dimensions.width`

- **ACCEPT** `dimensions.height`

- **ACCEPT** `dimensions.unit`

- **ACCEPT** `measurements.punchDiameter`

- **ACCEPT** `punchDiameter.value`

- **ACCEPT** `punchDiameter.unit`

- **ACCEPT** `measurements.holeSeparation`

- **ACCEPT** `holeSeparation.value`

- **ACCEPT** `measurements.margins`

- **ACCEPT** `margins.treble`

- **ACCEPT** `margins.bass`

- **ACCEPT** `margins.unit`

- **ACCEPT** `measurements.measuredBy`

- **ACCEPT** `measuredBy.software`

- **ACCEPT** `measuredBy.version`

- **ACCEPT** `measuredBy.date`

- **EDIT** `RollCopy.production` — replace TSDoc comment with:
  > The production event that created this roll copy.
  > @see lrm:R28i was produced by

- **EDIT** `RollCopy.location` — replace TSDoc comment with:
  > The current physical location or archive where this copy is held.
  > @see crm:P55 has current location

## `src/Edition.ts`

- **EDIT** `EditionCreation.publisher` — replace TSDoc comment with:
  > The person or institution responsible for publishing the edition.
  > @see crm:P14 carried out by

- **EDIT** `EditionCreation.publicationDate` — replace TSDoc comment with:
  > The date on which the edition was published.
  > @format date
  > @see crm:P4 has time-span

- **ACCEPT** `EditionCreation.collationTolerance`

- **ACCEPT** `RollTempo` (interface)

- **ACCEPT** `RollTempo.startsWith`

- **ACCEPT** `RollTempo.endsWith`

- **ACCEPT** `RollTempo.unit`

- **EDIT** `Edition.creation` — replace TSDoc comment with:
  > Information about the creation of this edition,
  > including publisher and publication date.
  > @see lrm:R17i was created by

- **ACCEPT** `Edition.copies`

- **ACCEPT** `Edition.versions`

- **ACCEPT** `Edition.tempoAdjustment`

## `src/ConditionState.ts`

- **EDIT** `ConditionState.description` — replace TSDoc comment with:
  > A free-text description of the condition, providing
  > details beyond the type classification.
  > @see crm:P3 has note

## `src/Collation.ts`

- **ACCEPT** `CollationTolerance.toleranceEnd`

